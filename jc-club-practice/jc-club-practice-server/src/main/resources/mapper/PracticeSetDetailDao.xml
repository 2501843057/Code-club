<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jingdianjichi.practice.server.dao.PracticeSetDetailDao">
    <insert id="insertBatch">
        insert into practice_set_detail
        (set_id, subject_id, subject_type, created_by, created_time, is_deleted)
        values
        <foreach collection="practiceSetDetailPOS" item="item" open="(" close=")" separator="," >
            (#{item.setId}, #{item.subjectId}, #{item.subjectType}, #{item.createdBy}, #{item.createdTime}, #{item.isDeleted})
        </foreach>
    </insert>
</mapper>

