spring:
  application:
    name: jc-club-gateway-dev
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: 47.120.54.15:8848
        prefix: ${spring.application.name}
        group: DEFAULT_GROUP
        namespace:
        file-extension: yaml
      discovery:
        enabled: true
        server-addr: 47.120.54.15:8848